const API='';let produtos=[],carrinho=[];
function fmt(v){return'R$ '+Number(v).toFixed(2).replace('.',',')}
function showToast(msg,tipo=''){const t=document.getElementById('toast');t.textContent=msg;t.className='toast show'+(tipo?' '+tipo:'');clearTimeout(t._t);t._t=setTimeout(()=>{t.className='toast'},2800)}
function showTab(tab,btn){document.querySelectorAll('.section').forEach(s=>s.classList.remove('active'));document.querySelectorAll('.tab').forEach(t=>t.classList.remove('active'));document.getElementById('tab-'+tab).classList.add('active');btn.classList.add('active');if(tab==='estoque')renderEstoque();if(tab==='historico')carregarHistorico();if(tab==='venda'){carregarProdutos();carregarResumo()}}
async function apiFetch(path,opts={}){const r=await fetch(API+'/api'+path,{headers:{'Content-Type':'application/json'},...opts});const d=await r.json();if(!r.ok)throw new Error(d.error||'Erro');return d}
async function carregarTudo(){try{await Promise.all([carregarProdutos(),carregarResumo()]);document.getElementById('status-conexao').textContent='● Online';document.getElementById('status-conexao').className='status-ok'}catch(e){document.getElementById('status-conexao').textContent='● Offline';document.getElementById('status-conexao').className='status-err'}}
async function carregarProdutos(){try{produtos=await apiFetch('/produtos');renderVenda();renderEstoque()}catch(e){document.getElementById('venda-lista').innerHTML=`<div class="error-msg">Erro: ${e.message}</div>`}}
function renderVenda(){const l=document.getElementById('venda-lista');if(!produtos.length){l.innerHTML='<div class="empty-state">Nenhum produto cadastrado</div>';return}l.innerHTML=produtos.map(p=>`<div class="venda-item ${p.estoque==0?'sem-estoque':''}" onclick="${p.estoque>0?`addCarrinho(${p.id})`:''}"><div><div class="vi-name">${p.emoji} ${p.nome}</div><div class="vi-info">${fmt(p.preco)} · ${p.estoque>0?p.estoque+' un.':'Sem estoque'}</div></div>${p.estoque>0?'<span class="vi-plus">+</span>':''}</div>`).join('')}
function renderEstoque(){const l=document.getElementById('stock-list');const al=produtos.filter(p=>p.estoque<=p.estoque_minimo).length;document.getElementById('m-produtos').textContent=produtos.length;document.getElementById('m-alertas').textContent=al;if(!produtos.length){l.innerHTML='<div class="empty-state">Nenhum produto ainda</div>';return}l.innerHTML=produtos.map(p=>{const low=p.estoque<=p.estoque_minimo;return`<div class="stock-item"><div class="stock-emoji">${p.emoji}</div><div class="stock-info"><div class="stock-name">${p.nome}${low?'<span class="badge-low">estoque baixo</span>':''}</div><div class="stock-sub">${fmt(p.preco)} · alerta em ${p.estoque_minimo} un.</div></div><div class="stock-qty"><button class="qty-btn" onclick="ajustarEstoque(${p.id},-1)">−</button><span class="qty-num ${low?'qty-low':''}">${p.estoque}</span><button class="qty-btn" onclick="ajustarEstoque(${p.id},+1)">+</button></div></div>`}).join('')}
async function ajustarEstoque(id,delta){try{const a=await apiFetch(`/produtos/${id}/estoque`,{method:'PATCH',body:JSON.stringify({delta})});const i=produtos.findIndex(p=>p.id===id);if(i>=0)produtos[i]=a;renderEstoque();renderVenda()}catch(e){showToast('Erro: '+e.message)}}
async function addProduto(){const nome=document.getElementById('new-name').value.trim();const preco=parseFloat(document.getElementById('new-price').value);const estoque=parseInt(document.getElementById('new-qty').value);const emoji=document.getElementById('new-emoji').value.trim()||'🍺';const estoque_minimo=parseInt(document.getElementById('new-alert').value)||5;if(!nome||isNaN(preco)||isNaN(estoque)){showToast('⚠ Preencha nome, preço e quantidade');return}try{const novo=await apiFetch('/produtos',{method:'POST',body:JSON.stringify({nome,emoji,preco,estoque,estoque_minimo})});produtos.push(novo);['new-name','new-price','new-qty','new-emoji','new-alert'].forEach(id=>{document.getElementById(id).value=''});renderEstoque();renderVenda();showToast('✓ '+nome+' adicionado!','green-toast')}catch(e){showToast('Erro: '+e.message)}}
function addCarrinho(id){const p=produtos.find(x=>x.id===id);if(!p||p.estoque===0)return;const ex=carrinho.find(c=>c.id===id);if(ex&&ex.qty>=(p.estoque)){showToast('⚠ Estoque insuficiente');return}if(ex)ex.qty++;else carrinho.push({id,qty:1});renderCarrinho()}
function removeCarrinho(id){const i=carrinho.findIndex(c=>c.id===id);if(i>=0){if(carrinho[i].qty>1)carrinho[i].qty--;else carrinho.splice(i,1)}renderCarrinho()}
function limparCarrinho(){carrinho=[];renderCarrinho()}
function renderCarrinho(){const itEl=document.getElementById('carrinho-itens');const emEl=document.getElementById('carrinho-empty');const ftEl=document.getElementById('carrinho-footer');if(!carrinho.length){itEl.innerHTML='';emEl.style.display='block';ftEl.style.display='none';return}emEl.style.display='none';ftEl.style.display='block';itEl.innerHTML=carrinho.map(c=>{const p=produtos.find(x=>x.id===c.id);return`<div class="carrinho-item"><span class="ci-name">${p.emoji} ${p.nome}</span><span class="ci-qty">×${c.qty}</span><span class="ci-price">${fmt(p.preco*c.qty)}</span><button class="ci-remove" onclick="removeCarrinho(${c.id})">✕</button></div>`}).join('');const total=carrinho.reduce((s,c)=>{const p=produtos.find(x=>x.id===c.id);return s+p.preco*c.qty},0);document.getElementById('carrinho-total').textContent=fmt(total)}
async function finalizarVenda(){if(!carrinho.length)return;const itens=carrinho.map(c=>({produto_id:c.id,quantidade:c.qty}));try{await apiFetch('/vendas',{method:'POST',body:JSON.stringify({itens})});const total=carrinho.reduce((s,c)=>{const p=produtos.find(x=>x.id===c.id);return s+p.preco*c.qty},0);carrinho=[];renderCarrinho();await carregarProdutos();await carregarResumo();showToast('✓ Venda de '+fmt(total)+' registrada!','green-toast')}catch(e){showToast('Erro: '+e.message)}}
async function carregarResumo(){try{const r=await apiFetch('/vendas/resumo');document.getElementById('m-vendas').textContent=r.total_vendas;document.getElementById('m-total').textContent='R$'+Math.round(r.total_arrecadado);document.getElementById('m-itens').textContent=r.total_itens;document.getElementById('h-vendas').textContent=r.total_vendas;document.getElementById('h-total').textContent='R$'+Math.round(r.total_arrecadado);document.getElementById('h-ticket').textContent='R$'+Math.round(r.ticket_medio)}catch{}}
async function carregarHistorico(){const l=document.getElementById('historico-lista');l.innerHTML='<div class="loading">Carregando...</div>';try{const v=await apiFetch('/vendas');await carregarResumo();if(!v.length){l.innerHTML='<div class="empty-state">🧾<br/>Nenhuma venda ainda</div>';return}l.innerHTML=v.map((venda,i)=>`<div class="historico-item"><span class="hist-num">#${v.length-i}</span><div class="hist-info"><div class="hist-desc">${venda.descricao}</div><div class="hist-hora">${new Date(venda.criado_em).toLocaleTimeString('pt-BR',{hour:'2-digit',minute:'2-digit'})}</div></div><span class="hist-total">${fmt(venda.total)}</span></div>`).join('')}catch(e){l.innerHTML=`<div class="error-msg">Erro: ${e.message}</div>`}}
document.addEventListener('DOMContentLoaded',carregarTudo);
setInterval(async()=>{const a=document.querySelector('.section.active').id;if(a==='tab-venda'){await carregarProdutos();await carregarResumo()}},30000);
